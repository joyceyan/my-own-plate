{
  "name": "myownplate-backend",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate:local": "wrangler d1 migrations apply myownplate-db --local",
    "migrate:remote": "wrangler d1 migrations apply myownplate-db --remote",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260721.0",
    "typescript": "^5.5.0",
    "wrangler": "^4.0.0"
  }
}
